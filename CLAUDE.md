# CLAUDE.md

作業前に知らないと失敗することだけを書く。経緯と決定理由は issue と PR に残す。

## このプロトタイプが示すこと

YOSS の支援の方向性 A / B / C のうち、**B「地域資源の活用」だけが実質的に機能していない**。
実物の校内チーム会議では B の項目（「居場所、こども食堂の活用」など）にチェックを入れられるが、
そこにあるのはカテゴリ名だけで、**具体的な団体名がシステムに無い**。原因は UI ではなく、
地域の支援団体がシステムの外側にいること。本プロトタイプは支援団体を YOSS に取り込み、
学校の入力を増やさずに支援サイクルを閉じる提案。

## 画面

| 呼び方 | ルート | ファイル | 誰向け |
| --- | --- | --- | --- |
| 画面A | `/register` | `pages/RegisterPage.tsx` | 支援団体（対応可能な支援を登録する） |
| 画面C | `/dashboard` | `pages/DashboardPage.tsx` | 共通（支援団体ディレクトリ） |
| 画面D | `/meeting` | `pages/MeetingPage.tsx` | 学校（校内チーム会議で支援候補を見る） |
| トップ | `/` | `pages/TopPage.tsx` | 提案の説明 |

会話でも issue でも「画面D」と呼ぶ。

## コマンド

```bash
npm run dev            # 開発サーバー（Vite）
npm run build          # tsc -b && vite build
npm run lint           # oxlint
npx tsc -b --noEmit    # 型チェックだけ
```

`noUnusedLocals` が有効なので、使わない import は型エラーになる。
コミット前に型チェック・lint・build の3つを通す。

## データの置き場所

バックエンドは無く、モックデータと localStorage だけで動く。

- `types/index.ts` … 型はすべてここ。新しい型を作る前に必ず既存を確認する
- `data/organizations.ts` … 支援団体のシードと、団体まわりの導出関数
- `data/screening.ts` … スクリーニング37項目と、回答から8領域スコアを出す導出関数
- `data/students.ts` … 児童のデモデータ。スコアは持たず `screening.ts` から導出する
- `data/records.ts` … 担当の自由記述・対応記録のデモデータ（問題タグから導出）
- `data/meeting.ts` … 会議の定数（年度・学期・方向性A/B/C・支援の現状の項目・タブ）
- `data/mockData.ts` … 画面Dの候補を組み立てる `getSuggestions()` と、領域ごとのグループ化
- `data/schools.ts` … 学校名と「ログイン中の学校」
- `data/organizationStore.ts` … 団体データの永続化（localStorage）
- `hooks/useOrganizationStore.ts` … 上記を `useSyncExternalStore` で購読する

**スコアの向き**：`Student.problems`（問題タグ）→ スクリーニング37項目の回答 → 8領域スコア。
実物と同じく「37項目の合計が領域スコア」なので、スコアを手で置かない。タグを足し引きすれば
スコアも支援候補も動く。領域ごとの満点は項目数で変わるため、表示のときだけ10点満点に正規化する。

画面をまたぐ状態は `organizationStore`、画面固有の選択状態は各ページの `useState`。

**YOSS 8領域**（`SupportCategory`）＝ 学校適応 / 学習 / 家庭状況 / 発達 / 健康 / 経済 / 福祉 / 地域情報。
児童・支援・レビューを繋ぐ共通言語。

## 設計判断（変えるなら理由を確認してから）

過去の issue で議論して決めたもの。壊すと以前の指摘に逆戻りする。

- **先生の入力欄を増やさない。** これが企画の主張そのもの。既存製品にある入力（会議メモ等）は
  使ってよいが、本プロトタイプで新しい入力欄を足さない
- **画面Dの外枠はビューポート高に収める。** ページ全体はスクロールさせない。ただし
  タブの中身は各タブが縦スクロールする。以前は「候補の一覧だけがスクロールし、領域の選択は
  常に見えたまま」にしていたが、決まった高さに情報を押し込むほうが窮屈で読めなかった
- **支援候補の抽出は8領域スコアだけで行う。** スコアの元はスクリーニング37項目の回答で、
  その回答は `Student.problems`（ProblemTag）から組み立てる。抽出のロジックが直接
  タグを見にいかない、という意味であって、タグは巡り巡って抽出に効く
- **レビューは支援単位で集計する。** 団体単位の集計は補足としてのみ使う
- **重複した情報を同じ画面に二度置かない。** 追加する前に、既に出ている情報を疑う

## 踏みやすい落とし穴

- **型を変えたら `organizationStore.migrate()` も直す。** localStorage には旧バージョンの形の
  データが残っている。フィールドを足す・変えるとき migrate を忘れると、保存済みデータを
  持つ環境だけが壊れる（実際に `SchoolReview` の変更で起きた）
- **シードを変えても、保存済みの環境には反映されない。** `organizations.ts` のシードを直しても、
  localStorage に自団体のデータがある環境では migrate が保存済みの内容を優先する（利用者の編集を
  上書きしないため、これは正しい挙動）。デモの前に画面Aの「初期状態に戻す」を押す
- **データの検証は `npx tsx` で使い捨てスクリプトを書くのが早い。** ただし
  **プロジェクトルートに置いて実行する**こと。`/tmp` からだと相対 import が解決できない
- **dev サーバーのポートは決め打ちにしない。** 5173 が利用者のサーバーで埋まっていることがあり、
  Vite が別のポートに回る。実際の値はサーバーのログで確認する
- **実物の YOSS に寄せる作業は、文章だけでは齟齬が出る。** スクリーンショットを issue に
  貼ってもらってから着手する

## UI の約束

- Tailwind v4。色とフォントは `src/index.css` の `@theme` に定義（`yoss-yellow` など）
- 選択状態は `bg-yoss-yellow-light` + `border-yoss-yellow`
- 実物の YOSS は紺系だが、本プロトタイプは黄色系で統一する
- 画面をまたいで使うものは `components/` に置く。同じ描画を2箇所に書かない
- ファイル構成を勝手に再編しない

## Git の約束

- ブランチ名は `feature/連番-スラッグ`。連番は **issue 番号ではなく作業の通し番号**
  （例：`feature/9-claude-md`）
- コミットメッセージは `feat:` / `refactor:` + 日本語の要約。本文に「なぜそうしたか」を書く
- issue を閉じるコミット・PR には `Closes #番号` を入れる
- `package-lock.json` に作業前からの差分が残っていることがある。関係ない変更は含めない
